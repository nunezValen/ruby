<p style="color: green"><%= notice %></p>

<% content_for :title, "Genres" %>

<h1>Genres</h1>

<div id="genres">
  <% @genres.each do |genre| %>
    <%= render genre %>
    <p>
      <%= link_to "Show this genre", [:backstore, genre] %>
    </p>
  <% end %>
</div>

<%= link_to "New genre", new_backstore_genre_path %>
