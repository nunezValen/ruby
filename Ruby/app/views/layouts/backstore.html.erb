<!DOCTYPE html>
<html lang="es">
  <head>
    <title><%= content_for(:title) || "Backstore" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="app-body">
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
      <div class="container py-2">
        <%= link_to "DiscoStore", backstore_products_path, class: "navbar-brand" %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#backstoreNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="backstoreNav">
          <ul class="navbar-nav ms-auto gap-2">
            <li class="nav-item">
              <%= link_to "Productos", backstore_products_path, class: "nav-link#{current_page?(backstore_products_path) ? ' active' : ''}" %>
            </li>
            <li class="nav-item">
              <%= link_to "Géneros", backstore_genres_path, class: "nav-link#{current_page?(backstore_genres_path) ? ' active' : ''}" %>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <main class="container py-4">
      <%= render "shared/flash" %>
      <%= yield %>
    </main>
  </body>
</html>
