<!DOCTYPE html>
<html lang="es">
  <head>
    <title><%= content_for(:title) || "Ruby" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Ruby">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="app-body" data-controller="confirm-modal">
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
      <div class="container py-2">
        <%= link_to root_path, class: "navbar-brand d-flex align-items-center" do %>
          <%= image_tag "Logo.png", alt: "DiscoStore", class: "navbar-logo" %>
        <% end %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="mainNav">
          <ul class="navbar-nav me-auto gap-2">
          <div class="d-flex align-items-center gap-2 ms-lg-3 mt-3 mt-lg-0">
            <% if user_signed_in? %>
              <%= link_to "Mi Perfil", edit_backstore_user_path(current_user), class: "btn btn-outline-light w-100 w-lg-auto" %>
            <% end %>
          </div>
          <div class="d-flex align-items-center gap-2 ms-lg-3 mt-3 mt-lg-0">
            <% if user_signed_in? %>
              <%= button_to "Cerrar sesión", destroy_user_session_path, method: :delete, class: "btn btn-outline-light w-100 w-lg-auto" %>
            <% else %>
              <%= link_to "Iniciar sesión", new_user_session_path, class: "btn btn-outline-light w-100 w-lg-auto" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <main class="container py-4">
      <%= render "shared/flash" %>
      <%= yield %>
    </main>

    <footer class="app-footer mt-5">
      <div class="container text-center small">
        Tecnologías de Producción de Software - Ruby | Desarrollado por
        Valentín Nuñez, Uziel Juárez Piñeiro y Zoe Eguaras
      </div>
    </footer>

    <div class="modal fade" tabindex="-1" data-confirm-modal-target="modal">
      <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content border border-2 border-dark rounded-4">
          <div class="modal-header">
            <h5 class="modal-title">Confirmar acción</h5>
            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
          </div>
          <div class="modal-body">
            <p class="mb-0" data-confirm-modal-target="message">
              ¿Estás seguro de que querés continuar?
            </p>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-outline-dark" data-bs-dismiss="modal">Cancelar</button>
            <button type="button" class="btn btn-primary" data-confirm-modal-target="confirmButton" data-action="click->confirm-modal#confirm">
              Confirmar
            </button>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>