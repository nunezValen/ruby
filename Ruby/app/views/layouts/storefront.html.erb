<!DOCTYPE html>
<html lang="es">
  <head>
    <title><%= content_for(:title) || "DiscoStore" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="app-body">
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
      <div class="container py-2">
        <%= link_to "DiscoStore", root_path, class: "navbar-brand" %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#storefrontNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="storefrontNav">
          <ul class="navbar-nav me-auto gap-2">
            <li class="nav-item">
              <%= link_to "Catálogo", storefront_path, class: "nav-link#{current_page?(storefront_path) && params[:media_type].blank? && params[:state].blank? ? ' active' : ''}" %>
            </li>
            <li class="nav-item">
              <%= link_to "Vinilos", storefront_path(media_type: "vinyl"), class: "nav-link#{params[:media_type] == 'vinyl' ? ' active' : ''}" %>
            </li>
            <li class="nav-item">
              <%= link_to "CDs", storefront_path(media_type: "cd"), class: "nav-link#{params[:media_type] == 'cd' ? ' active' : ''}" %>
            </li>
            <li class="nav-item">
              <%= link_to "Nuevos", storefront_path(state: "new_item"), class: "nav-link#{params[:state] == 'new_item' ? ' active' : ''}" %>
            </li>
            <li class="nav-item">
              <%= link_to "Usados", storefront_path(state: "used_item"), class: "nav-link#{params[:state] == 'used_item' ? ' active' : ''}" %>
            </li>
          </ul>

          <div class="d-flex align-items-center gap-2 ms-lg-3 mt-3 mt-lg-0">
            <% if user_signed_in? %>
              <%= link_to "Administración", backstore_products_path, class: "btn btn-outline-light w-100 w-lg-auto" %>
            <% end %>
          </div>
          <div class="d-flex align-items-center gap-2 ms-lg-3 mt-3 mt-lg-0">
            <% if user_signed_in? %>
              <%= button_to "Cerrar sesión", destroy_user_session_path, method: :delete, class: "btn btn-outline-light w-100 w-lg-auto" %>
            <% else %>
              <%= link_to "Acceso empleados", new_user_session_path, class: "btn btn-outline-light w-100 w-lg-auto" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <main class="container py-4">
      <%= render "shared/flash" %>
      <%= yield %>
    </main>

    <footer class="border-top py-4 mt-5">
      <div class="container text-center">
        <p class="mb-1 text-muted">DiscoStore - Tu tienda de vinilos y CDs</p>
        <small class="text-muted">&copy; 2025 - Visítanos en nuestra tienda física</small>
      </div>
    </footer>
  </body>
</html>